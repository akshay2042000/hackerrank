#include <stdio.h>
/* Include other headers as needed */
int main()
{
    int i=1,n,sum,k;
  scanf("%d",&n);
  
  while(i<=n)
  {
    sum=0;
    k=1;
    if(i==1)
    {
      printf("1\n");
    }
    else
    {
      while(k!=i+1)
      {
        sum=sum*10+k;
        k++;
      }
      k--;
      while(k!=1)
      {
        k--;
        sum=sum*10+k;
      }
   printf("%d\n",sum);
    }
       
    i++;
  }
    return 0;
}
